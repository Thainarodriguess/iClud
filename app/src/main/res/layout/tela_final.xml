<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fundo_principal"
    android:padding="24dp">

    <LinearLayout
        android:id="@+id/blocoFinal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/txtParabens"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="Parabéns!"
            android:textColor="@color/coral"
            android:textSize="42sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/txtConclusao"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:gravity="center"
            android:text="Você completou o iClud!"
            android:textColor="@color/titulo"
            android:textSize="22sp"
            android:textStyle="bold" />

    </LinearLayout>

    <Button
        android:id="@+id/btnMenuPrincipal"
        android:layout_width="240dp"
        android:layout_height="52dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="55dp"
        android:backgroundTint="@color/botao_verde"
        android:text="VOLTAR AO MENU PRINCIPAL"
        android:textColor="@color/texto_botao"
        android:textSize="12sp"
        android:textStyle="bold" />

</RelativeLayout>